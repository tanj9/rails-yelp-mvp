<div class="header py-3 px-2 my-2">
  <h1 class="mb-4">Add a review to <%= @restaurant.name %></h1>
</div>

<%= simple_form_for [ @restaurant, @review ] do |f| %>
  <%= f.input :rating, collection: (0..5) %>
  <%= f.input :content %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
